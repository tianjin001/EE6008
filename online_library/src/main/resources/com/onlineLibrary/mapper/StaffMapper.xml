<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.onlineLibrary.mapper.StaffMapper">

    <insert id="add">
    insert into staff(username,password)
    values(#{username}, #{password})
    </insert>

    <select id="select" resultType="com.onlineLibrary.entity.Staff">
    select * from staff where
    username = #{username}
    and password = #{password}
    </select>

    <select id="selectByCondition" resultType="com.onlineLibrary.entity.Staff">
    select * from staff
    where username = #{username}
    </select>

</mapper>